import { style } from '@vanilla-extract/css';

export const textAreaContainerStyle = style({
  display: 'flex',
  flexDirection: 'column',
  gap: '0.8rem',
  width: '100%',
  padding: '1.2rem 0',
});
