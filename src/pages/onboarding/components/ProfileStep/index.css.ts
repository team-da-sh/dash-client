import { style } from '@vanilla-extract/css';

export const profileStyle = style({
  position: 'relative',

  margin: '39.5 auto 2.4 auto',
});

export const icCameraStyle = style({
  position: 'absolute',
  bottom: '0.6rem',
  right: '0.6rem',
});
